&НаСервере
Процедура ПостроитьТаблицуДанных(ДанныеГостя)    
	тзТаблицаЗапроса=Новый ТаблицаЗначений;
	Если НЕ ЭтаФорма.Элементы.Найти("РезультатЗапроса") = Неопределено Тогда  
		ЭтаФорма.РезультатЗапроса.Очистить();   
		Для каждого элМ из ДанныеГостя Цикл    
			Для каждого св из элМ Цикл    
				тзТаблицаЗапроса.Колонки.Добавить(св.Ключ);
			КонецЦикла;
			Прервать;
		КонецЦикла;
		
	Иначе 
		
		МассивТипаВыбора = Новый Массив;      
		МассивТипаВыбора.Добавить(Тип("ТаблицаЗначений"));  
		
		ОписаниеТипаВыбораТаблицаЗначений = Новый ОписаниеТипов(МассивТипаВыбора);		
		
		МассивРеквизитов = Новый Массив;
		
		РеквизитТаблица = Новый РеквизитФормы("РезультатЗапроса", ОписаниеТипаВыбораТаблицаЗначений,,"Данные вывода");
		
	//__________________________________________________
	//__________________________________________________
		
		МассивТипаВыбора = Новый Массив;
		
		МассивТипаВыбора.Добавить(Тип("Строка"));  	  
		МассивТипаВыбора.Добавить(Тип("Число")); 
		МассивТипаВыбора.Добавить(Тип("СправочникСсылка.ПодарочныеСертификаты"));

		ОписаниеТипаВыбораСтрока = Новый ОписаниеТипов(МассивТипаВыбора);	
		
		
		МассивРеквизитов.Добавить(РеквизитТаблица);
		
		
		//
		Для каждого элМ из ДанныеГостя Цикл    
			Для каждого св из элМ Цикл    
				Колонка = Новый РеквизитФормы(св.Ключ, ОписаниеТипаВыбораСтрока, "РезультатЗапроса", Строка(св.Ключ));
				МассивРеквизитов.Добавить(Колонка);   
				
				тзТаблицаЗапроса.Колонки.Добавить(св.Ключ);
				
			КонецЦикла;
			Прервать;
		КонецЦикла;
		
		ЭтаФорма.ИзменитьРеквизиты(МассивРеквизитов);
		
		ЭлементТовары = ЭтаФорма.Элементы.Добавить("РезультатЗапроса",Тип("ТаблицаФормы"));
		ЭлементТовары.ПутьКДанным = "РезультатЗапроса";
		Для каждого элМ из ДанныеГостя Цикл    
			Для каждого св из элМ Цикл    
				Попытка
					ЭлементДанные = ЭтаФорма.Элементы.Добавить("ТЕСТ"+св.Ключ,Тип("ПолеФормы"),ЭлементТовары);
					ЭлементДанные.Вид = ВидПоляФормы.ПолеВвода;
					ЭлементДанные.ПутьКДанным = "РезультатЗапроса."+Строка(св.Ключ);
				Исключение
					Сообщить("св.Ключ:"+св.Ключ) ;
				КонецПопытки;
				
			КонецЦикла;
			Прервать;
		КонецЦикла;   
	КонецЕсли ; 	
	Для каждого элМ из ДанныеГостя Цикл    
		стрГостя= тзТаблицаЗапроса.Добавить();
		Для каждого св из элМ Цикл    
			Попытка
				стрГостя[св.Ключ]=св.Значение;
			Исключение
				Сообщить("св.Ключ:"+св.Ключ) ;
			КонецПопытки;
		КонецЦикла;
	КонецЦикла;  
	
	ЗначениеВРеквизитФормы(тзТаблицаЗапроса, "РезультатЗапроса");
	
КонецПроцедуры



&НаКлиенте
Асинх  Процедура ФайлНачалоВыбора(Элемент, ДанныеВыбора, ВыборДобавлением, СтандартнаяОбработка)
	//ДиалогВыбора = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.Открытие);
	////ДиалогВыбора.Фильтр = "Текстовый документ (*.txt)|*.txt";
	//
	//Обещание = ДиалогВыбора.ВыбратьАсинх();
	//
	//ВыбранныеВДиалогеФайлы = Ждать Обещание;
	//
	//Если ВыбранныеВДиалогеФайлы = Неопределено Тогда
	//	Возврат;
	//Конецесли;
	//
	//ИмяФайла = ВыбранныеВДиалогеФайлы[0];
	//Объект.Файл = ИмяФайла;
	//ТабличныйДокумент = Новый ТабличныйДокумент;
	//
	//Обещание = ТабличныйДокумент.ПрочитатьАсинх(ИмяФайла, КодировкаТекста.UTF8);
	//
	//РезультатЧтения = Ждать Обещание;
	//
	//СодержимоеВФайле = ТабличныйДокумент.ПолучитьОбласть();
	////ТекстовыйДок = Новый ТекстовыйДокумент;
	////
	////Обещание = ТекстовыйДок.ПрочитатьАсинх(Файл, КодировкаТекста.UTF8);
	////
	////РезультатЧтения = Ждать Обещание;
	////
	////СодержимоеВФайле = ТекстовыйДок.ПолучитьТекст();   
	
	
	АдресФайлаВоВременномХранилище = "";
	
	РежимДиалога = РежимДиалогаВыбораФайла.Открытие;
	ВыборФайла = Новый ДиалогВыбораФайла(РежимДиалога);
	РасшФильтр = "(*.xls,*.xlsx)     |*.xls;*.xlsx;";
	Фильтр = НСтр("ru = 'Excel'; en = 'Excel'") + РасшФильтр;
	ВыборФайла.Фильтр = Фильтр;
	ВыборФайла.Заголовок = "Выберите файл Excel:";
	
	ОписаниеОповещения = Новый ОписаниеОповещения("ЗагрузитьДанныеЗавершение", ЭтотОбъект);
	НачатьПомещениеФайла(ОписаниеОповещения, АдресФайлаВоВременномХранилище,
	ВыборФайла, Истина, УникальныйИдентификатор);
	
КонецПроцедуры

&НаСервере
Процедура ЗагрузитьДанныеЗавершение(Результат, Адрес, ВыбранноеИмяФайла, ДополнительныеПараметры)  Экспорт
	
	//Файл = ВыбранноеИмяФайла;
	ИмяФайла = ПолучитьИмяВременногоФайла("xlsx");
	Файл = ИмяФайла;
	
	ДвоичныеДанные = ПолучитьИзВременногоХранилища(Адрес);
	ДвоичныеДанные.Записать(ИмяФайла);
	
	    //1) Выводим Excel в табличн.документ
    ТаблДок = Новый ТабличныйДокумент;
    ТаблДок.Прочитать(ВыбранноеИмяФайла, СпособЧтенияЗначенийТабличногоДокумента.Значение);

    //2) Выгружаем в таблицу значений
    ПЗ = Новый ПостроительЗапроса;
    ПЗ.ИсточникДанных = Новый ОписаниеИсточникаДанных(ТаблДок.Область());
    ПЗ.ДобавлениеПредставлений = ТипДобавленияПредставлений.НеДобавлять;
    ПЗ.ЗаполнитьНастройки();
	ПЗ.Выполнить();
	ТаблицаЗначений = ПЗ.Результат.Выгрузить(); 
	МассДанных = Новый Массив; 
	к = 1;
	Для каждого стр из ТаблицаЗначений Цикл
		СК = ОбщегоНазначения.СтрокаТаблицыЗначенийВСтруктуру(стр);  
		СК.Вставить("нп",к);
		к = к+1;
		МассДанных.Добавить(СК);
	КонецЦикла;
	ПостроитьТаблицуДанных(МассДанных);
КонецПроцедуры
